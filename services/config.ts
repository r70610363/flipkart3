
// Global Configuration for API and Backend

// 1. When you have a backend, set this to true
export const ENABLE_API = false; 

// 2. Put your Backend URL here (e.g., "https://my-api.render.com/api")
// If running locally with Node, usually: "http://localhost:5000/api"
export const API_BASE_URL = "http://localhost:5000/api";

// 3. Mock Latency (to simulate real internet speed in dev mode)
export const MOCK_DELAY = 600;
